name: test-rust

on:
  release:
    types: [published]

jobs:
  check:
    if: startsWith(github.ref, 'refs/tags/rust/')
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: packages/rust
    steps:
      - uses: actions/checkout@v4
      - uses: actions-rust-lang/setup-rust-toolchain@v1

      - name: build
        run: cargo build --release

      - name: test
        run: cargo test

      - name: install cargo-llvm-cov
        run: cargo install cargo-llvm-cov --locked

      - name: coverage
        run: cargo llvm-cov --all-features --tests --lcov --output-path lcov.info

      - name: check threshold
        run: |
          LINE=$(grep -oP 'TOTAL.*?[\d.]+(?=%)' lcov.info | tail -1)
          echo "line coverage: ${LINE}%"
          awk -v line="$LINE" 'BEGIN { if (line + 0 < 95) { print "FAIL: coverage " line "% < 95%"; exit 1 } }'
          echo "PASS: coverage ${LINE}% >= 95%"
